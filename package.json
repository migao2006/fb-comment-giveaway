{
  "name": "fb-comment-giveaway",
  "version": "0.3.5",
  "private": true,
  "type": "module",
  "description": "在 Facebook 目前頁面本機擷取留言並抽獎的 Bookmarklet",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "node scripts/build.mjs --watch"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/jsdom": "^30.0.0",
    "esbuild": "^0.28.2",
    "jsdom": "^30.0.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
